# User Preferences

- Whenever producing a visual/HTML deliverable (animations, designs, pages), always publish it as an Artifact immediately so it renders inline in the chat, rather than only sending a file. Do this automatically, without being asked each time.
